# Custom capability definition
name: "minReportingInterval"
id: "instantforge19660.minReportingInterval"
version: 1

attributes:
  minInterval:
    schema:
      type: integer

commands:
  setMinInterval:
    arguments:
      - name: value
        schema:
          type: integer
