# Presentation for minReportingInterval
dashboard:
  states:
    - label: "{{minReportingInterval.minInterval}} seconds"

detailView:
  - label: Minimum Reporting Interval
    value: "{{minReportingInterval.minInterval}} seconds"
