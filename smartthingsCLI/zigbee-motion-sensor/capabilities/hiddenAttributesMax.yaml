# Custom capability definition
name: "maxReportingInterval"
id: "instantforge19660.maxreportinginterval"
version: 1
presentation: maxReportingInterval_presentation

attributes:
  maxInterval:
    schema:
      type: integer
    default: 30

commands:
  setMaxInterval:
    arguments:
      - name: value
        schema:
          type: integer
