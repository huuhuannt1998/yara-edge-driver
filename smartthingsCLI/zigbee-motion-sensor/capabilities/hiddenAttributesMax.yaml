name: "maxReportingInterval"
id: "namespace.maxReportingInterval"
version: 1
attributes:
  maxInterval:
    schema:
      type: "integer"
    setter: "setMaxInterval"
    enumCommands: []
commands:
  setMaxInterval:
    name: "setMaxInterval"
    arguments:
      - name: "value"
        schema:
          type: "integer"
