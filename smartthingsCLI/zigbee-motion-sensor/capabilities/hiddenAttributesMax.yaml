# similarly definition for maxReportingInterval
name: "maxReportingInterval"
id: "app.maxReportingInterval"

attributes:
  maxInterval:
    schema:
      type: integer

commands:
  setMaxInterval:
    arguments:
      - name: value
        schema:
          type: integer
