# Presentation for maxReportingInterval
dashboard:
  states:
    - label: "{{maxReportingInterval.maxInterval}} seconds"

detailView:
  - label: Maximum Reporting Interval
    value: "{{maxReportingInterval.maxInterval}} seconds"
