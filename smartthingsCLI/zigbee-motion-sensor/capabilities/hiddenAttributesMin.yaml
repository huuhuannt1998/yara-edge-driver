# Custom capability definition
name: "minReportingInterval"
id: "instantforge19660.minreportinginterval"
version: 1

attributes:
  minInterval:
    schema:
      type: integer
    default: 30

commands:
  setMinInterval:
    arguments:
      - name: value
        schema:
          type: integer
