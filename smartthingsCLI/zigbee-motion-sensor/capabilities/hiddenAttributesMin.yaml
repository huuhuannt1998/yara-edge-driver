name: "minReportingInterval"
id: "namespace.minReportingInterval"
version: 1
attributes:
  minInterval:
    schema:
      type: "integer"
    setter: "setMinInterval"
    enumCommands: []
commands:
  setMinInterval:
    name: "setMinInterval"
    arguments:
      - name: "value"
        schema:
          type: "integer"
