# Custom capability definition
name: "minReportingInterval"
id: "app.minReportingInterval"
version: 1

attributes:
  minInterval:
    schema:
      type: integer

commands:
  setMinInterval:
    arguments:
      - name: value
        schema:
          type: integer
